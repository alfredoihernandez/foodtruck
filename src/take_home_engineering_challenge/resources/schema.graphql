type FoodTruck {
    id: ID,
    name: String!,
    type: String,
    address: String!,
    permitStatus: String!,
    cuisineDesc: String,
    latitude: Float,
    longitude: Float,
    distance: Float
}

type QueryRoot {
    foodTrucks: [FoodTruck]
}

schema { query: QueryRoot }